# Mac/MPS development requirements — for local dev, testing, and small-model inference only.
# Actual SFT/DPO training requires CUDA (bitsandbytes). Use requirements.txt inside Docker.

# PyTorch — latest stable with MPS support on Apple Silicon
torch>=2.3.0
torchvision>=0.18.0
torchaudio>=2.3.0

# HuggingFace ecosystem — compatible with Python 3.14
transformers>=4.44.2
peft>=0.12.0
trl>=0.10.1
datasets>=2.21.0
accelerate>=0.33.0
tokenizers>=0.19.1
huggingface-hub>=0.24.6
safetensors>=0.4.4

# bitsandbytes excluded — CUDA-only, not available on Mac

# Structured output / constrained decoding
# outlines excluded — outlines_core requires Rust + no Python 3.14 wheel; use Docker for constrained-decoding eval
instructor>=1.3.3

# Experiment tracking
wandb>=0.17.8
mlflow>=2.15.1

# Data / storage (s3fs/aiobotocore excluded — version conflicts on Mac; use Docker for S3)
boto3>=1.35.7
pyarrow>=17.0.0

# Evaluation helpers
jsonschema>=4.23.0
nltk>=3.9.1
rouge-score>=0.1.2
scikit-learn>=1.5.1
numpy>=1.26.4
pandas>=2.2.2

# Config / CLI
pyyaml>=6.0.2
omegaconf>=2.3.0
click>=8.1.7
rich>=13.7.1

# Env / reproducibility
python-dotenv>=1.0.1

# Dev / testing
pytest>=8.3.2
pytest-cov>=5.0.0
